pipeline {
    agent any

    environment {
        JMETER_HOME = "C:\\Apache\\apache-jmeter-5.6.3"
        RESULTS_DIR = "results"
        REPORT_API = "report_api"
        REPORT_WEB = "report_web"
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main',
                url: 'https://github.com/bavishasundaram29-lang/jenkins-pipeline-demo-BOth_API_and_Web.git'
            }
        }

        stage('Run API Test') {
            steps {
                bat """
                mkdir %REPORT_API%
                mkdir %RESULTS_DIR%\\api

                jmeter -n -t api_test.jmx ^
                -l %RESULTS_DIR%\\api\\api.jtl ^
                -e -o %REPORT_API%
                """
            }
        }

        stage('Run Web Test') {
            steps {
                bat """
                mkdir %REPORT_WEB%
                mkdir %RESULTS_DIR%\\web

                jmeter -n -t web_test.jmx ^
                -l %RESULTS_DIR%\\web\\web.jtl ^
                -e -o %REPORT_WEB%
                """
            }
        }

        stage('Archive Reports') {
            steps {
                archiveArtifacts artifacts: '''
                    report_api/**,
                    report_web/**,
                    results/**
                ''', fingerprint: true
            }
        }

        stage('Publish HTML Reports (Jenkins UI)') {
            steps {
                publishHTML([
                    reportDir: 'report_api',
                    reportFiles: 'index.html',
                    reportName: 'API HTML Report',
                    keepAll: true,
                    allowMissing: false,
                    alwaysLinkToLastBuild: true
                ])

                publishHTML([
                    reportDir: 'report_web',
                    reportFiles: 'index.html',
                    reportName: 'Web HTML Report',
                    keepAll: true,
                    allowMissing: false,
                    alwaysLinkToLastBuild: true
                ])
            }
        }
    }

    post {
        always {
            emailext (
                subject: "Jenkins API & Web Test Report - Build ${env.BUILD_NUMBER}",
                body: """
                Hi Team,

                Please find attached the API and Web JMeter HTML reports.

                Build Number: ${env.BUILD_NUMBER}
                Status: ${currentBuild.currentResult}

                Regards,
                Jenkins
                """,
                to: "yourmail@gmail.com",
                mimeType: 'text/html',

                attachmentsPattern: '''
                    report_api/index.html,
                    report_web/index.html
                '''
            )
        }
    }
}
